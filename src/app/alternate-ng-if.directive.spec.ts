import { AlternateNgIFDirective } from './alternate-ng-if.directive';

describe('AlternateNgIFDirective', () => {
  it('should create an instance', () => {
    const directive = new AlternateNgIFDirective();
    expect(directive).toBeTruthy();
  });
});
