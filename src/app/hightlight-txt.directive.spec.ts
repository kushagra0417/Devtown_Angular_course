import { HightlightTxtDirective } from './hightlight-txt.directive';

describe('HightlightTxtDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightTxtDirective();
    expect(directive).toBeTruthy();
  });
});
